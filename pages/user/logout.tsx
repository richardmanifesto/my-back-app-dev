import React from "react"

/**
 * Render the login page.
 *
 * @param {Field} fields
 *   The form fields to render.
 *
 * @constructor
 */
export default function Logout({fields}) {
  return <div />
}

export async function getStaticProps() {
  return {
    props: {
    }
  }
}
